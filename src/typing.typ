#import "libs/elembic/lib.typ" as e
#import "components/grid.typ": grid
#import "components/label.typ": label
#import "components/title.typ": title
#import "components/legend.typ": legend

#let set_ = e.set_
#let set-grid = e.set_.with(grid)
#let set-title = e.set_.with(title)
#let set-label = e.set_.with(label)
#let set-legend = e.set_.with(legend)